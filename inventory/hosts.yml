all:
  children:
    proxmox_nodes:
      hosts:
        proxmox-node-a:
          ansible_host: 192.168.10.10
          ansible_user: root
          proxmox_node_name: proxmox-node-a
          guest_ids: [101, 102]
          soft_reboot_guest_order: [102, 101]
    docker_hosts:
      hosts:
        apps-vm:
          ansible_host: 192.168.10.20
          ansible_user: ops
          compose_root: /srv/compose
          maintenance_tier: apps
          guest_type: qemu
          proxmox_guest_id: 102
    guests:
      children:
        docker_hosts: {}
